### pass compilation 

To build the pass
```
cmake -H. -Bbuild
```

### run pass on sample
by default
```
./run.py ## this will run the pass on sample/sample/sample.c by default
```

on other scripts
```
./run.py myscript.c
```
